title: Filosofía Antugua
description: Ingresar a clases
template:
  repo: filosofia-antugua-template
  name: filosofia-antugua
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
